// the x11 window manager - composite redirect, WL_SURFACE_ID association,
// icccm input models, the fake single-desktop ewmh some games insist on,
// float heuristics. prefers WM_DELETE_WINDOW, kills only as a last resort.
